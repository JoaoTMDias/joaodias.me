<div class="sub-section">
  <h3 class="sr-only">Bio</h3>
  <div class="bio">
    <div class="bio__picture">
      <img class="bio__image" src="/BioPicture.jpg" width="300" height="554" alt="João Dias sitting in a chair, in his grandparents garden, on a sunny day.">
    </div>
    <div class="bio__content">
      <h4 class="bio__title">I focus on creating great, accessible and rich web experiences for everyone. I’ve been
        lucky enough to have been doing that since 2011.</h4>
      <p class="bio__paragraph">“He’s a pretty chilled dude”, thats what my friends would say. Besides working as a
        Senior Frontend Engineer, I also teach web development classes at first and second year students on the
        Polythecnic Institute of Coimbra.</p>
      <p class="bio__paragraph">On my free time I usually go out and explore the fantastic world of craft beers, rock
        out to some great live music and hit the gym whenever I can.</p>
    </div>
  </div>
</div>

<style lang="scss">
  .bio {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    gap: 2rem;
    width: 100%;

    @media (min-width: 60rem) {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      gap: 1rem;
    }

    &__picture {
      position: relative;
      width: fit-content;

      &:after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: transparent;
        border: 2px solid var(--text-color);
        transform: translate3d(6vw, 4vh, 0) scale(1);
        pointer-events: none;
        transition: transform 200ms ease-in-out;
        border-radius: 4px;


        @media (min-width: 60rem) {
          transform: translate3d(3rem, 1.5rem, 0) scale(1.025);
        }
      }

      @media (prefers-reduced-motion: no-preference) {
        &:hover:after {
          @media (min-width: 60rem) {
            transform: translate3d(2rem, 1rem, 0) scale(1.025);
          }
        }
      }


      @media (min-width: 60rem) {
        grid-column-start: 2;
        grid-column-end: 5;
      }
    }

    &__image {
      border-radius: 2px;
    }

    &__content {
      @media (min-width: 60rem) {
        grid-column-start: 7;
        grid-column-end: 12;
      }
    }

    &__title {
      font-family: var(--heading-font-family);
      font-style: normal;
      font-weight: 500;
      font-size: 2rem;
      line-height: 112%;
      margin-block: 3rem;
    }

    &__paragraph {
      font-family: var(--text-font-family);
      font-style: normal;
      font-weight: 300;
      font-size: clamp(1.2rem, 6vw, 1.375rem);
      line-height: 1.2727272727;

      +.bio__paragraph {
        margin-top: 1.5rem;
      }
    }
  }
</style>
